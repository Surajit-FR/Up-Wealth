const swiper = {
    // Provide dummy implementations for the methods or properties you're using
    // For example:
    Swiper: jest.fn(() => ({
        slideNext: jest.fn(),
        slidePrev: jest.fn(),
    })),
};

export default swiper;